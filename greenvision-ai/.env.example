# Point the deployed frontend at the HuggingFace Space backend.
# Create this as plain ".env" when deploying to Vercel (Settings -> Environment Variables),
# or locally while testing against a remote backend.
VITE_API_URL=https://<your-username>-greenvision-ai.hf.space